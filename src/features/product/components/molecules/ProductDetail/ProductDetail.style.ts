import { css } from "@emotion/css";

export const productDetailStyle = css({
  width: 200,
  height: "auto",

  padding: "8px 16px",
  position: "relative",

  backgroundColor: '#fff',
  '&:hover': {
    cursor: "pointer"
  },
});